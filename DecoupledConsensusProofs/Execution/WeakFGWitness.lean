module
public import DecoupledConsensusProofs.Execution.ModelVocabulary

/-!
# `WeakFGWitnessRun` —  archive stub

The earlier source of this module was retired under  to
`the compatibility layer`
(byte-exact). This stub keeps the module name resolvable for the aggregate
and its out-of-cone consumers so that `lake build` can compute its jobs; it
exports no declarations. Consumers that still need the retired declarations
are red by design until they are restated to the named runtime.
-/
